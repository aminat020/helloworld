name: HelloWorld
on: push
jobs:
  check-bats-version:
    runs-on: ubuntu-latest # VM ubuntu pour faire tourner ubuntu
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm i -g bats
      - run: bats -v